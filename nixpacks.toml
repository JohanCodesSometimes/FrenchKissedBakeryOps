[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip", "python312Packages.virtualenv", "ffmpeg", "exiftool"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m venv .venv",
  ".venv/bin/python -m pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "npm start"
