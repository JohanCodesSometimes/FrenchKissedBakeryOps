[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip", "ffmpeg", "exiftool"]

[phases.install]
cmds = ["npm ci", "pip install --user --no-cache-dir -r requirements.txt"]

[start]
cmd = "npm start"
